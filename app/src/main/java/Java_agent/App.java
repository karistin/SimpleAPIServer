/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package Java_agent;

import Entity.DataSet;
import org.objectweb.asm.ClassReader;
import org.objectweb.asm.util.TraceClassVisitor;
import repo.DataSetRepo;
import repo.DataSetRepoMemory;

import java.io.IOException;

import java.io.PrintWriter;
import java.io.StringWriter;
import java.lang.String;
import java.util.*;

public class App {
    public static void main(String[] args) throws IOException {
        DataSetRepo taskRepository = new DataSetRepoMemory();
        List<String> file = Arrays.asList("java_agent.SoccerPlayer","java_agent.App");
        ClassPrinter cp = new ClassPrinter();
        ClassReader cr = new ClassReader("java_agent.SoccerPlayer");
        cr.accept(cp, 0);

        taskRepository.save(cp.dataset);

        DataSet test = taskRepository.find_class_data("Java_agent/SoccerPlayer").get();
        test.print_dataset();

//        ClassReader reader = new ClassReader("java_agent.KoreaSoccerPlayer");
//        StringWriter sw = new StringWriter();
//        TraceClassVisitor tcv = new TraceClassVisitor(new PrintWriter(System.out));
//        reader.accept(tcv, 0);

    }
}
